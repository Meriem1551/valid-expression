#include "main.h"

/**
 * isempty - checks if a stack is empty
 * @p: pointer to stack
 * Return: true if empty, false otherwise
 */

bool isempty(stack *p)
{
    if (p == NULL)
        return true;
    else
        return false;
}